# CPS01 Android App

This project is an Android application that includes a camera screen and a login interface. The login information is stored in a local JSON file.

## Building

Ensure you have the Android SDK installed and `sdk.dir` configured in `local.properties`. Then run:

```sh
./gradlew build
```

This will compile the project and run unit tests.
